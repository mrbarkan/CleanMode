{
  "name": "cleanmode-eventtap",
  "version": "1.0.0",
  "private": true,
  "description": "macOS CGEventTap for CleanMode's Cleaning Mode",
  "main": "index.js",
  "os": ["darwin"],
  "scripts": {
    "build": "prebuildify --napi --strip --arch arm64 --target 22.0.0 && prebuildify --napi --strip --arch x64 --target 22.0.0",
    "build-current-arch": "prebuildify --napi --strip --target 22.0.0"
  }
}
